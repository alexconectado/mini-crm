# ============================================================================
# CRM - Environment Variables Template
# ============================================================================
# 
# IMPORTANTE: Criar arquivos .env e .env.dev baseado neste template
# NUNCA commitar .env com dados reais!
#
# ============================================================================

# Django Configuration
# ===================
DJANGO_SECRET_KEY=seu-secret-key-aleatorio-aqui-mudar-em-producao
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration
# =====================
# Use SQLite para desenvolvimento rápido
# Use PostgreSQL para produção e testes com Docker
USE_POSTGRES=False

# Se usar PostgreSQL:
DB_HOST=localhost
DB_NAME=crm_db
DB_USER=crm_user
DB_PASSWORD=senha-segura-aqui
DB_PORT=5432

# Redis Configuration
# ===================
REDIS_URL=redis://127.0.0.1:6379/1

# Security (SSL/HTTPS)
# ====================
# False = desenvolvimento local (HTTP)
# True = produção com Traefik (HTTPS)
SECURE_SSL_REDIRECT=False
SESSION_COOKIE_SECURE=False
CSRF_COOKIE_SECURE=False

# Traefik / Let's Encrypt
# =======================
# Necessário para deployment com Traefik em produção
LETSENCRYPT_EMAIL=seu-email@exemplo.com

# ============================================================================
# INSTRUÇÕES DE USO:
# ============================================================================
#
# 1. DESENVOLVIMENTO LOCAL (com runserver):
#    cp .env.example .env
#    vim .env
#    # Manter DEBUG=True, SQLite, sem HTTPS
#    python manage.py runserver
#
# 2. TESTES COM DOCKER LOCAL:
#    cp .env.example .env.dev
#    vim .env.dev
#    # Manter DEBUG=True, PostgreSQL (db), Redis (redis)
#    docker-compose -f docker-compose.local.yml up
#
# 3. PRODUÇÃO COM TRAEFIK:
#    cp .env.example .env
#    vim .env
#    # Manter DEBUG=False, PostgreSQL, HTTPS, LETSENCRYPT_EMAIL válido
#    docker-compose -f docker-compose.traefik.yml up -d
#
# ============================================================================
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
