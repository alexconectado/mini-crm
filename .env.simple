# ==============================================================================
# CRM - Environment Configuration (Auto-Detect Development vs Production)
# ==============================================================================
#
# SIMPLE APPROACH: Apenas declare o NECESSÁRIO
# settings.py detecta automaticamente se é dev ou produção
#
# ==============================================================================

# Django Secret Key (mude em produção!)
DJANGO_SECRET_KEY=seu-secret-key-mude-em-producao-aleatorio-aqui

# Environment Detection (deixar vazio = desenvolvimento local)
# NUNCA declare DEBUG aqui. settings.py detecta automaticamente:
# - Se for http://localhost:8000 (runserver) → DEBUG=True, sem HTTPS
# - Se for https://seu-dominio.com (produção) → DEBUG=False, com HTTPS

# For production ONLY, uncomment:
# DEBUG=False

# Allowed hosts (separados por vírgula)
ALLOWED_HOSTS=localhost,127.0.0.1

# Database (opcional, padrão é SQLite)
# Se não definir, usa SQLite (perfeito para dev)
# Para produção, remova o # e configure PostgreSQL:
# USE_POSTGRES=True
# DB_HOST=postgres
# DB_NAME=crm_db
# DB_USER=crm_user
# DB_PASSWORD=senha-segura
# DB_PORT=5432

# Redis (opcional, padrão é cache em memória)
# REDIS_URL=redis://redis:6379/1

# Let's Encrypt Email (APENAS para produção com Traefik)
# LETSENCRYPT_EMAIL=seu-email@dominio.com
